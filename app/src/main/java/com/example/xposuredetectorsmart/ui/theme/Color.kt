package com.example.xposuredetectorsmart.ui.theme

import androidx.compose.ui.graphics.Color

// Brand — Signal Cyan, the app's HUD/glow accent
val SignalCyan = Color(0xFF00D9C0)
val SignalCyanDim = Color(0xFF00A896)
val SignalCyanBright = Color(0xFF5CFFE8)

// Semantic status colors — shared by gauges, chips, banners, badges, and chart limit lines
val StatusSafe = Color(0xFF22C55E)
val StatusWarning = Color(0xFFF5A623)
val StatusCritical = Color(0xFFEF4444)

// Light surfaces
val SurfaceLightBg = Color(0xFFF7F8FA)
val SurfaceLightCard = Color(0xFFFFFFFF)
val SurfaceLightOutline = Color(0xFFE2E5E9)
val OnSurfaceLight = Color(0xFF14181B)
val OnSurfaceLightMuted = Color(0xFF5B646B)

// Dark surfaces — near-black instrument panel
val SurfaceDarkBg = Color(0xFF0A0D0F)
val SurfaceDarkCard = Color(0xFF10161A)
val SurfaceDarkOutline = Color(0xFF2A343A)
val OnSurfaceDark = Color(0xFFECF2F3)
val OnSurfaceDarkMuted = Color(0xFF8B9BA1)

// HUD grid/scanline overlay tint (drawn at low alpha over screen backgrounds)
val HudGridLine = Color(0xFF00D9C0)
